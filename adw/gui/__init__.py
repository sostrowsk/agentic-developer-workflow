"""Read-side of the ADW event log (GUI-SPEC §11, steps 6–7).

Pure, importable building blocks with no web layer: the incremental log
``reader``, the span-tree ``model`` and the repo ``registry``.
"""
